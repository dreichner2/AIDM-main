global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: aidm-backend
    metrics_path: /api/metrics/prometheus
    static_configs:
      - targets:
          - host.docker.internal:5050
        labels:
          service: ai-dm
